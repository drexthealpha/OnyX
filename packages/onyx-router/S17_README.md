/**
 * @onyx/router — S17 Complete Source Files
 * 
 * All files written for SESSION 17:
 * - package.json with deps (better-sqlite3, zod)
 * - tsconfig.json 
 * - src/types.ts (pure TypeScript interfaces, no zod)
 * - src/index.ts (barrel export)
 * - src/x402.ts (x402 micropayment via @onyx/vault)
 * - src/routing/providers.ts (55+ providers with real pricing)
 * - src/routing/strategy.ts (SIMPLE/MEDIUM/COMPLEX/REASONING tier classification)
 * - src/routing/load-balancer.ts (circuit breaker, round-robin)
 * - src/budget/tracker.ts (SQLite spend tracking)
 * - src/budget/cap.ts (budget cap enforcement + kernel alarm)
 * - src/budget/per-user-meter.ts (token efficiency RL reporting)
 * - tests/router.test.ts (vitest tests)
 */